name: CI/CD Pipeline

on:
  push:
    branches:
    - main
  pull:
    branches:
    - main

jobs:
  build:
    name: Build using Make
    runs-on: ubuntu-latest
    steps:
      - name: Install Makefile and GCC compiler
        cpu_quota: ZZ
